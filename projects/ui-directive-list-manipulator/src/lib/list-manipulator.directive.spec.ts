import { ListManipulatorDirective } from './list-manipulator.directive';

describe('ListManipulatorDirective', () => {
  it('should create an instance', () => {
    const directive = new ListManipulatorDirective();
    expect(directive).toBeTruthy();
  });
});
